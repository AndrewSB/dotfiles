# Arguments: takes an optional param of platform name.
# If no argument is given then carthage builds for all platforms.

function deps() {
	if [ $# -eq 0 ]; then
		carthage update --no-use-binaries 
	else
		carthage update --no-use-binaries --platform $@
	fi
}
